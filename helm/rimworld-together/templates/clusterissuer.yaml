apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: "{{ template "rimworld-together.fullname" . }}-letsencrypt-prod"
  labels:
    {{- include "rimworld-together.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: {{ .Values.email }}
    privateKeySecretRef:
      name: "{{ template "rimworld-together.fullname" . }}-letsencrypt-prod"
    solvers:
      - dns01:
          route53:
            region: eu-west-3
            accessKeyIDSecretRef: 
              name: "{{ template "rimworld-together.fullname" . }}-dns-secrets"
              key: AWS_ACCESS_KEY_DNS
            hostedZoneID: {{ .Values.certificate.hostedZoneID }}
            secretAccessKeySecretRef:
              name: "{{ template "rimworld-together.fullname" . }}-dns-secrets"
              key: AWS_SECRET_KEY_DNS
---
kind: Secret
apiVersion: v1
metadata:
  name: "{{ template "rimworld-together.fullname" . }}-dns-secrets"
  namespace: cert-manager
  labels:
{{ include "rimworld-together.labels" . | indent 4 }}
data:
  AWS_SECRET_KEY_DNS: {{ .Values.certificate.AWS_SECRET_KEY_DNS | b64enc }}
  AWS_ACCESS_KEY_DNS: {{ .Values.certificate.AWS_ACCESS_KEY_DNS | b64enc }}